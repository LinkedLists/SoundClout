import React from 'react';

export default function() {
  // 404 component is being scrapped
  // right now this is being repurposed as an empty component for the auth route
  return <></>
    // <h1 className="error">Turn back. No pokemon can be found on this route!!!</h1>
  
}